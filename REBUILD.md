# REBUILD.md — إعادة بناء واجهة vMeeting على نظام UlaSpace

> اقرأ هذا الملف بالكامل قبل تغيير أي سطر. اشتغل على مرحلة واحدة في كل مرة، وارفع commit منفصل لكل مرحلة، ولا تنتقل للمرحلة التالية قبل أن تمر فحوصات المرحلة الحالية.

## القاعدة الأولى: لا تحذف أي وظيفة

هذه إعادة بناء **بصرية**. ممنوع حذف أو تعديل:

- أي route، controller، model، migration، أو job
- أي منطق JavaScript: WebRTC، الحركة على الخريطة، الحضور المباشر، الإشعارات، المحادثة، رفع الملفات، الدفع
- أي prop أو slot في مكوّنات Blade الموجودة
- أي `wire:`، `x-data`، `@click`، `id`، `name`، `data-*`، أو `class` يستخدمه JS للاستهداف

قبل تعديل أي ملف، ابحث فيه عن المعرّفات التي يستهدفها JS واحفظها كما هي. إذا احتجت تغيير هيكل عنصر يستهدفه سكربت، عدّل السكربت في نفس الـ commit وقل ذلك صريحاً في رسالة الـ commit.

**اختبار القبول لكل مرحلة:** الوظيفة التي كانت تعمل قبل التعديل تعمل بعده بنفس الطريقة.

---

## المصادر — بهذا الترتيب

| الملف | ما فيه |
| :--- | :--- |
| `DESIGN_SYSTEM.md` | النظام كاملاً: 226 متغيّراً، 47 نمط نص، 9 تأثيرات، 45 مجموعة مكوّنات، مقاسات كل مكوّن |
| `resources/design-system/tokens.json` | نفس القيم بصيغة يقرأها الكود — استخدمه للتحقق الآلي |
| `resources/css/ulaspace-tokens.css` | التطبيق الفعلي: كل رمز كخاصية CSS، الوضعان الفاتح والداكن |
| `CLAUDE.md` | القواعد الملزمة |
| `FIX_BRIEF.md` | المخالفات المرصودة على الموقع المباشر |

مصدر الحق هو `DESIGN_SYSTEM.md`. الملفات الثلاثة القديمة (`ULASPACE_DESIGN_SYSTEM_GUIDE.md`, `DESIGN_SYSTEM_IMPLEMENTATION.md`, `FIGMA_DESIGN_SYSTEM_AUDIT.md`) تصف قيماً قبل الترحيل — احذفها في المرحلة صفر.

---

## المرحلة 0 — خط الأساس

1. أنشئ فرعاً: `git checkout -b ulaspace-rebuild`
2. سجّل الوضع الحالي — هذه أرقامك المرجعية:

```bash
grep -rn -- "--nx-\|--brand-\|--bg-base\|--text-main" resources/ | wc -l
grep -rnE "#[0-9a-fA-F]{6}" resources/views/ | wc -l
grep -rn 'href="#"' resources/views/ | wc -l
grep -rnP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" resources/views/ | wc -l
```

3. `npm run build` — يجب أن ينجح قبل أن تبدأ. إذا فشل، أصلح البناء أولاً وارفع commit منفصلاً.
4. احذف الملفات الثلاثة القديمة واستبدلها بإشارة إلى `DESIGN_SYSTEM.md`.
5. تأكد أن `CLAUDE.md`, `DESIGN_SYSTEM.md`, `FIX_BRIEF.md`, `REBUILD.md` موجودة في جذر المستودع.

**فحص المرحلة:** البناء ينجح، والأرقام الأربعة مسجّلة.

---

## المرحلة 1 — طبقة الرموز

الملفان `resources/css/ulaspace-tokens.css` و `resources/design-system/tokens.json` مولّدان من ملف Figma. **لا تعدّلهما يدوياً.**

تحقّق فقط:

1. كل متغيّر في `tokens.json` له `cssVar` مقابل موجود فعلاً في `ulaspace-tokens.css`. اكتب سكربت node مؤقتاً يقارنهما ويطبع الفروق — يجب أن تكون صفراً.
2. كل رمز دلالي في مجموعة `Color` له قيمة في الوضعين `Light` و `Dark`.
3. `app.css` يستورد `ulaspace-tokens.css` أولاً، ثم `tailwindcss`، وجسر `@theme` موجود.
4. لا توجد لوحة ألوان ثانية في أي ملف CSS. ابحث عن تعريفات `hsl(` و `--brand-`:

```bash
grep -rn "hsl(\|--brand-" resources/css/
```

**فحص المرحلة:** الفروق صفر، والوضعان مكتملان، ولا لوحة ثانية.

---

## المرحلة 2 — المكوّنات الستة عشر

المكوّنات في `resources/views/components/` أُعيدت كتابتها لتطابق Figma. مهمتك التحقق لا إعادة الكتابة.

لكل مكوّن، افتح `DESIGN_SYSTEM.md` §8 وقارن: الارتفاع، الحشو الأفقي، نصف القطر، المسافة بين العناصر، مقاس الأيقونة، ورمز اللون في كل حالة (default / hover / press / focus / disabled).

| المكوّن | المقاس المطلوب |
| :--- | :--- |
| `btn` | ارتفاع 36/44/52 · حشو 16/22/26 · قطر 10/14/18 · فراغ 8 |
| `icon-btn` | 40×40 · قطر 12 · أيقونة 20 |
| `badge` (Tag) | ارتفاع 29 · حشو 12 · فراغ 6 · pill · نقطة 7 |
| `badge variant=filter` (Chip) | ارتفاع 36 · حشو 16 · pill |
| `input` | ارتفاع 46 · قطر 14 · حشو 16 |
| `checkbox` | مربع 20 · قطر 6 · حدود 1.5 · فراغ 11 |
| `switch` | مسار 44×26 · حشو 3 · مقبض 20 |
| `kpi-card` | قطر 20 · حشو 20/18 · فراغ 14 |
| `meeting-row` | قطر 14 · حشو 14/12 · مربع حالة 36 قطر 10 |
| `quick-tile` | قطر 16 · حشو 16 · أيقونة 26 |
| `card` | قطر 20 · ظل XS |
| `modal` | قطر 28 · ظل XL · غطاء `surface/overlay` + طمس 14 |
| `table`, `tabs`, `empty-state`, `donut-chart` | راجع §8 |

ثم:

- كل حالة تركيز تعرض حلقة ذهبية: `box-shadow: var(--ula-focus-ring)`
- لا عنصر تفاعلي أصغر من 44px
- `tactile-btn` هو الوحيد الذي لم يُعد بناؤه — أعد بناءه الآن على الرموز، مع الحفاظ على أسماء الأصناف التي يستهدفها JS

**فحص المرحلة:** لا hex خام في أي مكوّن، ولا `--nx-*`، وكل prop موجود كما كان.

---

## المرحلة 3 — إصلاحات `FIX_BRIEF.md`

نفّذ الأقسام 1 إلى 6 بالترتيب. **توقّف واسألني في:**

- **القسم 1** — الشعارات الأربعة في `public/images/` بلون العاج فقط وغير مرئية على الأسطح الفاتحة. أحتاج منك نسخة خضراء (`--ula-brand-mark-green`). إن لم تستطع إعادة تلوين PNG بنظافة، قل لي وسأصدّر SVG من Figma.
- **القسم 6** — لا يوجد قسم أسعار في الصفحة الرئيسية مع أن الروابط تشير إليه. إما نبنيه أو نحذف الروابط. لا تخمّن.

**فحص المرحلة:** الفحوصات الأربعة في §7 من `FIX_BRIEF.md` نظيفة.

---

## المرحلة 4 — الشاشات، واحدة واحدة

بهذا الترتيب بالضبط. بعد كل شاشة: commit، وتأكيد أن كل زر ورابط ونموذج فيها يعمل.

1. `landing/home.blade.php` + `landing/layout.blade.php`
2. `auth/login.blade.php`, `auth/register.blade.php`, `layouts/auth.blade.php`
3. `dashboard.blade.php` + `dashboard/partials/*` (16 تبويباً — تبويب في كل commit)
4. `office.blade.php` + `office/partials/modals.blade.php` (الأكبر، 364KB — قسّمه)
5. `projects/hub.blade.php` + `projects/partials/modals.blade.php`
6. `editor.blade.php`
7. `billing/payment.blade.php`, `guest_join.blade.php`
8. `superadmin/*` (13 ملفاً)

لكل شاشة:

- استبدل كل markup مبنيّ يدوياً بمكوّن موجود (`<x-btn>`, `<x-badge>`, `<x-input>`, `<x-card>`, `<x-modal>`, `<x-kpi-card>`, `<x-meeting-row>`, `<x-quick-tile>`, `<x-donut-chart>`, `<x-empty-state>`, `<x-table>`, `<x-tabs>`)
- كل لون ومسافة وقطر وزمن من رمز
- خصائص منطقية فقط: `margin-inline-start` لا `margin-left`
- كل رقم ووقت وسعر داخل نص عربي: `direction: ltr; unicode-bidi: isolate` و `var(--ula-font-mono)`
- كل عنوان: سطر عربي أساسي + سطر إنجليزي مصاحب أصغر وأخف (`.ula-headline-group`)
- لا إيموجي — أيقونات Material Symbols Rounded فقط

**بعد كل شاشة، اختبر الوظائف:** كل زر ينقر، كل نموذج يُرسل، كل modal يفتح ويغلق، كل رابط يذهب لمكانه، وكل سلوك JS (الحركة على الخريطة، الحضور، المحادثة، المكالمة) كما كان.

**فحص المرحلة:** الشاشة تعمل بالكامل وتطابق النظام في أربع حالات: فاتح+RTL، فاتح+LTR، داكن+RTL، داكن+LTR.

---

## المرحلة 5 — التحقق النهائي

```bash
# 1. لا أسماء رموز متقاعدة (يُستثنى بلوك التوافق أسفل ulaspace-tokens.css فقط)
grep -rn -- "--nx-\|--brand-\|--bg-base\|--text-main\|--accent-gradient" resources/

# 2. لا hex خام في العروض
grep -rnE "#[0-9a-fA-F]{6}" resources/views/

# 3. لا إيموجي
grep -rnP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" resources/views/

# 4. لا روابط ميتة
grep -rn 'href="#"' resources/views/

# 5. لا خصائص فيزيائية في CSS جديد
grep -rn "margin-left\|margin-right\|padding-left\|padding-right\|\bleft:\|\bright:" resources/views/ resources/css/

# 6. البناء
npm run build

# 7. الاختبارات
php artisan test
```

الفحوصات 1-5 يجب أن تعود فارغة، و6-7 ناجحة.

ثم على المتصفح، في الحالات الأربع، لكل شاشة:

- حلقة التركيز الذهبية ظاهرة على كل عنصر تفاعلي
- لا هدف لمس أصغر من 44px
- الشعار مقروء على السطح الذي يجلس عليه
- الأرقام والأوقات تُقرأ من اليسار لليمين داخل الجمل العربية
- التخطيط يعمل معكوساً بلا تراكب
- نص الجسم بتباين 4.5:1 على الأقل

---

## ما هو معروف ولا يُعتبر خطأ

- **خطوط Cairo غير موجودة** في المستودع. `ulaspace-tokens.css` يعرّف `@font-face` لـ `public/fonts/cairo/cairo-arabic.woff2` و `cairo-latin.woff2`؛ حتى تُضاف، يرجع Cairo إلى IBM Plex Sans Arabic. **لا تحذف قواعد `@font-face`.** أضف الملفين (أوزان 300–700) إن استطعت.
- **الأيقونات** Material Symbols Rounded نائبة عن مجموعة Figma المكوّنة من 42 أيقونة.
- **الصور** شرائط نائبة (`media/stripe-*`) في كل موضع يطلب صورة حقيقية.
- **بلوك التوافق** أسفل `ulaspace-tokens.css` مقصود؛ احذفه فقط عندما يعود الفحص الأول فارغاً تماماً.

---

## رسائل الـ commit

```
ulaspace: <المرحلة> — <ما تغيّر>

- التغييرات البصرية بالتفصيل
- الوظائف المتحقّق منها: <القائمة>
- فحوصات ناجحة: <الأرقام>
```

مثال:

```
ulaspace: phase 4.2 — auth screens on design system

- login/register/layout على الرموز الدلالية، حقول 46px قطر 14
- الشعار الأخضر على الأسطح الفاتحة
- أسماء الباقات عربية مع سطر إنجليزي مصاحب، الأسعار معزولة الاتجاه
- الوظائف المتحقّق منها: تسجيل الدخول، إنشاء حساب، التحقق من الحقول، اختيار الباقة
- فحوصات ناجحة: 1-6
```

---

## متى تتوقف وتسأل

- الشعارات الخضراء (المرحلة 3، القسم 1)
- قسم الأسعار: نبنيه أم نحذف روابطه (المرحلة 3، القسم 6)
- أي قيمة تحتاجها وليست في `DESIGN_SYSTEM.md` — لا تخترع رمزاً
- أي موضع يتطلب فيه مطابقة Figma تغيير سلوك أو حذف وظيفة
- أي تعارض بين `DESIGN_SYSTEM.md` وما تراه في الكود

لا تكمل على تخمين. قل ما وجدته واسأل.
